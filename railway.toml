[build]
  command = "pip install -r requirements.txt"

[start]
  command = "PORT=8080 python dashboard_web.py"

[deployHealthcheck]
  path = "/health"
  port = 8080
  interval = 10
  timeout = 5

[settings]
  port = 8080
  healthcheck = true